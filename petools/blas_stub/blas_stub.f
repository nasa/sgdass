      SUBROUTINE DASUM()
! ************************************************************************
! *                                                                      *
! *   These are so-called BLAS stub routines. They are provided to       *
! *   resolve depdendeces on blas routines in the case if petools is     *
! *   compiled without BLAS support. If a user process tries to access   *
! *   to one of these routines, they generate error message, unwind      *
! *   stack and abnorammly terminate.                                    *
! *                                                                      *
! *  ### 15-DEC-2006               v1.0 (c)  L. Petrov  15-DEC-2006 ###  *
! *                                                                      *
! ************************************************************************
      IMPLICIT   NONE 
      INTEGER*4  IARR(2), IND
      WRITE ( 6, '(A)' ) 'Trap of internal control: attempt to access '// &
     &                   'to blas routine DASUM. PETOOLS library was '// &
     &                   'configured without blas support'
      WRITE ( 6, '(A)' ) 'Abnoraml termination'
      IND = 3
      IND = IARR(IND) 
      CALL EXIT ( 1 )
      RETURN
      END SUBROUTINE DASUM
!
! ------------------------------------------------------------------------
!
      SUBROUTINE DASUM_()
      IMPLICIT   NONE 
      INTEGER*4  IARR(2), IND
      WRITE ( 6, '(A)' ) 'Trap of internal control: attempt to access '// &
     &                   'to blas routine DASUM. PETOOLS library was '// &
     &                   'configured without blas support'
      WRITE ( 6, '(A)' ) 'Abnoraml termination'
      IND = 3
      IND = IARR(IND) 
      CALL EXIT ( 1 )
      RETURN
      END SUBROUTINE DASUM_
!
! ------------------------------------------------------------------------
!
      SUBROUTINE DAXPY()
      IMPLICIT   NONE 
      INTEGER*4  IARR(2), IND
      WRITE ( 6, '(A)' ) 'Trap of internal control: attempt to access '// &
     &                   'to blas routine DAXPY. PETOOLS library was '// &
     &                   'configured without blas support'
      WRITE ( 6, '(A)' ) 'Abnoraml termination'
      IND = 3
      IND = IARR(IND) 
      CALL EXIT ( 1 )
      RETURN
      END SUBROUTINE DAXPY
!
! ------------------------------------------------------------------------
!
      SUBROUTINE DAXPY_()
      IMPLICIT   NONE 
      INTEGER*4  IARR(2), IND
      WRITE ( 6, '(A)' ) 'Trap of internal control: attempt to access '// &
     &                   'to blas routine DAXPY. PETOOLS library was '// &
     &                   'configured without blas support'
      WRITE ( 6, '(A)' ) 'Abnoraml termination'
      IND = 3
      IND = IARR(IND) 
      CALL EXIT ( 1 )
      RETURN
      END SUBROUTINE DAXPY_
!
! ------------------------------------------------------------------------
!
      SUBROUTINE DCOPY()
      IMPLICIT   NONE 
      INTEGER*4  IARR(2), IND
      WRITE ( 6, '(A)' ) 'Trap of internal control: attempt to access '// &
     &                   'to blas routine DCOPY. PETOOLS library was '// &
     &                   'configured without blas support'
      WRITE ( 6, '(A)' ) 'Abnoraml termination'
      IND = 3
      IND = IARR(IND) 
      CALL EXIT ( 1 )
      RETURN
      END SUBROUTINE DCOPY
!
! ------------------------------------------------------------------------
!
      SUBROUTINE DCOPY_()
      IMPLICIT   NONE 
      INTEGER*4  IARR(2), IND
      WRITE ( 6, '(A)' ) 'Trap of internal control: attempt to access '// &
     &                   'to blas routine DCOPY. PETOOLS library was '// &
     &                   'configured without blas support'
      WRITE ( 6, '(A)' ) 'Abnoraml termination'
      IND = 3
      IND = IARR(IND) 
      CALL EXIT ( 1 )
      RETURN
      END SUBROUTINE DCOPY_
!
! ------------------------------------------------------------------------
!
      SUBROUTINE DDOT()
      IMPLICIT   NONE 
      INTEGER*4  IARR(2), IND
      WRITE ( 6, '(A)' ) 'Trap of internal control: attempt to access '// &
     &                   'to blas routine DDOT. PETOOLS library was '// &
     &                   'configured without blas support'
      WRITE ( 6, '(A)' ) 'Abnoraml termination'
      IND = 3
      IND = IARR(IND) 
      CALL EXIT ( 1 )
      RETURN
      END SUBROUTINE DDOT
!
! ------------------------------------------------------------------------
!
      SUBROUTINE DDOT_()
      IMPLICIT   NONE 
      INTEGER*4  IARR(2), IND
      WRITE ( 6, '(A)' ) 'Trap of internal control: attempt to access '// &
     &                   'to blas routine DDOT. PETOOLS library was '// &
     &                   'configured without blas support'
      WRITE ( 6, '(A)' ) 'Abnoraml termination'
      IND = 3
      IND = IARR(IND) 
      CALL EXIT ( 1 )
      RETURN
      END SUBROUTINE DDOT_
!
! ------------------------------------------------------------------------
!
      SUBROUTINE DGEMM()
      IMPLICIT   NONE 
      INTEGER*4  IARR(2), IND
      WRITE ( 6, '(A)' ) 'Trap of internal control: attempt to access '// &
     &                   'to blas routine DGEMM. PETOOLS library was '// &
     &                   'configured without blas support'
      WRITE ( 6, '(A)' ) 'Abnoraml termination'
      IND = 3
      IND = IARR(IND) 
      CALL EXIT ( 1 )
      RETURN
      END SUBROUTINE DGEMM
!
! ------------------------------------------------------------------------
!
      SUBROUTINE DGEMM_()
      IMPLICIT   NONE 
      INTEGER*4  IARR(2), IND
      WRITE ( 6, '(A)' ) 'Trap of internal control: attempt to access '// &
     &                   'to blas routine DGEMM. PETOOLS library was '// &
     &                   'configured without blas support'
      WRITE ( 6, '(A)' ) 'Abnoraml termination'
      IND = 3
      IND = IARR(IND) 
      CALL EXIT ( 1 )
      RETURN
      END SUBROUTINE DGEMM_
!
! ------------------------------------------------------------------------
!
      SUBROUTINE DGEMV()
      IMPLICIT   NONE 
      INTEGER*4  IARR(2), IND
      WRITE ( 6, '(A)' ) 'Trap of internal control: attempt to access '// &
     &                   'to blas routine DGEMV. PETOOLS library was '// &
     &                   'configured without blas support'
      WRITE ( 6, '(A)' ) 'Abnoraml termination'
      IND = 3
      IND = IARR(IND) 
      CALL EXIT ( 1 )
      RETURN
      END SUBROUTINE DGEMV
!
! ------------------------------------------------------------------------
!
      SUBROUTINE DGEMV_()
      IMPLICIT   NONE 
      INTEGER*4  IARR(2), IND
      WRITE ( 6, '(A)' ) 'Trap of internal control: attempt to access '// &
     &                   'to blas routine DGEMV. PETOOLS library was '// &
     &                   'configured without blas support'
      WRITE ( 6, '(A)' ) 'Abnoraml termination'
      IND = 3
      IND = IARR(IND) 
      CALL EXIT ( 1 )
      RETURN
      END SUBROUTINE DGEMV_
!
! ------------------------------------------------------------------------
!
      SUBROUTINE DGER()
      IMPLICIT   NONE 
      INTEGER*4  IARR(2), IND
      WRITE ( 6, '(A)' ) 'Trap of internal control: attempt to access '// &
     &                   'to blas routine DGER. PETOOLS library was '// &
     &                   'configured without blas support'
      WRITE ( 6, '(A)' ) 'Abnoraml termination'
      IND = 3
      IND = IARR(IND) 
      CALL EXIT ( 1 )
      RETURN
      END SUBROUTINE DGER
!
! ------------------------------------------------------------------------
!
      SUBROUTINE DGER_()
      IMPLICIT   NONE 
      INTEGER*4  IARR(2), IND
      WRITE ( 6, '(A)' ) 'Trap of internal control: attempt to access '// &
     &                   'to blas routine DGER. PETOOLS library was '// &
     &                   'configured without blas support'
      WRITE ( 6, '(A)' ) 'Abnoraml termination'
      IND = 3
      IND = IARR(IND) 
      CALL EXIT ( 1 )
      RETURN
      END SUBROUTINE DGER_
!
! ------------------------------------------------------------------------
!
      SUBROUTINE DSCAL()
      IMPLICIT   NONE 
      INTEGER*4  IARR(2), IND
      WRITE ( 6, '(A)' ) 'Trap of internal control: attempt to access '// &
     &                   'to blas routine DSCAL. PETOOLS library was '// &
     &                   'configured without blas support'
      WRITE ( 6, '(A)' ) 'Abnoraml termination'
      IND = 3
      IND = IARR(IND) 
      CALL EXIT ( 1 )
      RETURN
      END SUBROUTINE DSCAL
!
! ------------------------------------------------------------------------
!
      SUBROUTINE DSCAL_()
      IMPLICIT   NONE 
      INTEGER*4  IARR(2), IND
      WRITE ( 6, '(A)' ) 'Trap of internal control: attempt to access '// &
     &                   'to blas routine DSCAL. PETOOLS library was '// &
     &                   'configured without blas support'
      WRITE ( 6, '(A)' ) 'Abnoraml termination'
      IND = 3
      IND = IARR(IND) 
      CALL EXIT ( 1 )
      RETURN
      END SUBROUTINE DSCAL_
!
! ------------------------------------------------------------------------
!
      SUBROUTINE DSPMV()
      IMPLICIT   NONE 
      INTEGER*4  IARR(2), IND
      WRITE ( 6, '(A)' ) 'Trap of internal control: attempt to access '// &
     &                   'to blas routine DSPMV. PETOOLS library was '// &
     &                   'configured without blas support'
      WRITE ( 6, '(A)' ) 'Abnoraml termination'
      IND = 3
      IND = IARR(IND) 
      CALL EXIT ( 1 )
      RETURN
      END SUBROUTINE DSPMV
!
! ------------------------------------------------------------------------
!
      SUBROUTINE DSPMV_()
      IMPLICIT   NONE 
      INTEGER*4  IARR(2), IND
      WRITE ( 6, '(A)' ) 'Trap of internal control: attempt to access '// &
     &                   'to blas routine DSPMV. PETOOLS library was '// &
     &                   'configured without blas support'
      WRITE ( 6, '(A)' ) 'Abnoraml termination'
      IND = 3
      IND = IARR(IND) 
      CALL EXIT ( 1 )
      RETURN
      END SUBROUTINE DSPMV_
!
! ------------------------------------------------------------------------
!
      SUBROUTINE DSYRK()
      IMPLICIT   NONE 
      INTEGER*4  IARR(2), IND
      WRITE ( 6, '(A)' ) 'Trap of internal control: attempt to access '// &
     &                   'to blas routine DSYRK. PETOOLS library was '// &
     &                   'configured without blas support'
      WRITE ( 6, '(A)' ) 'Abnoraml termination'
      IND = 3
      IND = IARR(IND) 
      CALL EXIT ( 1 )
      RETURN
      END SUBROUTINE DSYRK
!
! ------------------------------------------------------------------------
!
      SUBROUTINE DSYRK_()
      IMPLICIT   NONE 
      INTEGER*4  IARR(2), IND
      WRITE ( 6, '(A)' ) 'Trap of internal control: attempt to access '// &
     &                   'to blas routine DSYRK. PETOOLS library was '// &
     &                   'configured without blas support'
      WRITE ( 6, '(A)' ) 'Abnoraml termination'
      IND = 3
      IND = IARR(IND) 
      CALL EXIT ( 1 )
      RETURN
      END SUBROUTINE DSYRK_
!
! ------------------------------------------------------------------------
!
      SUBROUTINE DTRMM()
      IMPLICIT   NONE 
      INTEGER*4  IARR(2), IND
      WRITE ( 6, '(A)' ) 'Trap of internal control: attempt to access '// &
     &                   'to blas routine DTRMM. PETOOLS library was '// &
     &                   'configured without blas support'
      WRITE ( 6, '(A)' ) 'Abnoraml termination'
      IND = 3
      IND = IARR(IND) 
      CALL EXIT ( 1 )
      RETURN
      END SUBROUTINE DTRMM
!
! ------------------------------------------------------------------------
!
      SUBROUTINE DTRMM_()
      IMPLICIT   NONE 
      INTEGER*4  IARR(2), IND
      WRITE ( 6, '(A)' ) 'Trap of internal control: attempt to access '// &
     &                   'to blas routine DTRMM. PETOOLS library was '// &
     &                   'configured without blas support'
      WRITE ( 6, '(A)' ) 'Abnoraml termination'
      IND = 3
      IND = IARR(IND) 
      CALL EXIT ( 1 )
      RETURN
      END SUBROUTINE DTRMM_
!
! ------------------------------------------------------------------------
!
      SUBROUTINE DTRMV()
      IMPLICIT   NONE 
      INTEGER*4  IARR(2), IND
      WRITE ( 6, '(A)' ) 'Trap of internal control: attempt to access '// &
     &                   'to blas routine DTRMV. PETOOLS library was '// &
     &                   'configured without blas support'
      WRITE ( 6, '(A)' ) 'Abnoraml termination'
      IND = 3
      IND = IARR(IND) 
      CALL EXIT ( 1 )
      RETURN
      END SUBROUTINE DTRMV
!
! ------------------------------------------------------------------------
!
      SUBROUTINE DTRMV_()
      IMPLICIT   NONE 
      INTEGER*4  IARR(2), IND
      WRITE ( 6, '(A)' ) 'Trap of internal control: attempt to access '// &
     &                   'to blas routine DTRMV. PETOOLS library was '// &
     &                   'configured without blas support'
      WRITE ( 6, '(A)' ) 'Abnoraml termination'
      IND = 3
      IND = IARR(IND) 
      CALL EXIT ( 1 )
      RETURN
      END SUBROUTINE DTRMV_
!
! ------------------------------------------------------------------------
!
      SUBROUTINE DTRSM()
      IMPLICIT   NONE 
      INTEGER*4  IARR(2), IND
      WRITE ( 6, '(A)' ) 'Trap of internal control: attempt to access '// &
     &                   'to blas routine DTRSM. PETOOLS library was '// &
     &                   'configured without blas support'
      WRITE ( 6, '(A)' ) 'Abnoraml termination'
      IND = 3
      IND = IARR(IND) 
      CALL EXIT ( 1 )
      RETURN
      END SUBROUTINE DTRSM
!
! ------------------------------------------------------------------------
!
      SUBROUTINE DTRSM_()
      IMPLICIT   NONE 
      INTEGER*4  IARR(2), IND
      WRITE ( 6, '(A)' ) 'Trap of internal control: attempt to access '// &
     &                   'to blas routine DTRSM. PETOOLS library was '// &
     &                   'configured without blas support'
      WRITE ( 6, '(A)' ) 'Abnoraml termination'
      IND = 3
      IND = IARR(IND) 
      CALL EXIT ( 1 )
      RETURN
      END SUBROUTINE DTRSM_
!
! ------------------------------------------------------------------------
!
      SUBROUTINE LSAME()
      IMPLICIT   NONE 
      INTEGER*4  IARR(2), IND
      WRITE ( 6, '(A)' ) 'Trap of internal control: attempt to access '// &
     &                   'to blas routine LSAME. PETOOLS library was '// &
     &                   'configured without blas support'
      WRITE ( 6, '(A)' ) 'Abnoraml termination'
      IND = 3
      IND = IARR(IND) 
      CALL EXIT ( 1 )
      RETURN
      END SUBROUTINE LSAME
!
! ------------------------------------------------------------------------
!
      SUBROUTINE LSAME_()
      IMPLICIT   NONE 
      INTEGER*4  IARR(2), IND
      WRITE ( 6, '(A)' ) 'Trap of internal control: attempt to access '// &
     &                   'to blas routine LSAME. PETOOLS library was '// &
     &                   'configured without blas support'
      WRITE ( 6, '(A)' ) 'Abnoraml termination'
      IND = 3
      IND = IARR(IND) 
      CALL EXIT ( 1 )
      RETURN
      END SUBROUTINE LSAME_
!
! ------------------------------------------------------------------------
!
      SUBROUTINE DGBTRF()
! ************************************************************************
! *                                                                      *
! *   These are so-called BLAS stub routines. They are provided to       *
! *   resolve depdendeces on blas routines in the case if petools is     *
! *   compiled without BLAS support. If a user process tries to access   *
! *   to one of these routines, they generate error message, unwind      *
! *   stack and abnorammly terminate.                                    *
! *                                                                      *
! *  ### 15-DEC-2006               v1.0 (c)  L. Petrov  15-DEC-2006 ###  *
! *                                                                      *
! ************************************************************************
      IMPLICIT   NONE 
      INTEGER*4  IARR(2), IND
      WRITE ( 6, '(A)' ) 'Trap of internal control: attempt to access '// &
     &                   'to blas routine DGBTRF. PETOOLS library was '// &
     &                   'configured without blas support'
      WRITE ( 6, '(A)' ) 'Abnoraml termination'
      IND = 3
      IND = IARR(IND) 
      CALL EXIT ( 1 )
      RETURN
      END SUBROUTINE DGBTRF
!
! ------------------------------------------------------------------------
!
      SUBROUTINE DGBTRF_()
! ************************************************************************
! *                                                                      *
! *   These are so-called BLAS stub routines. They are provided to       *
! *   resolve depdendeces on blas routines in the case if petools is     *
! *   compiled without BLAS support. If a user process tries to access   *
! *   to one of these routines, they generate error message, unwind      *
! *   stack and abnorammly terminate.                                    *
! *                                                                      *
! *  ### 15-DEC-2006               v1.0 (c)  L. Petrov  15-DEC-2006 ###  *
! *                                                                      *
! ************************************************************************
      IMPLICIT   NONE 
      INTEGER*4  IARR(2), IND
      WRITE ( 6, '(A)' ) 'Trap of internal control: attempt to access '// &
     &                   'to blas routine DGBTRF_. PETOOLS library was '// &
     &                   'configured without blas support'
      WRITE ( 6, '(A)' ) 'Abnoraml termination'
      IND = 3
      IND = IARR(IND) 
      CALL EXIT ( 1 )
      RETURN
      END SUBROUTINE DGBTRF_
!
! ------------------------------------------------------------------------
!
      SUBROUTINE DGBTRS()
! ************************************************************************
! *                                                                      *
! *   These are so-called BLAS stub routines. They are provided to       *
! *   resolve depdendeces on blas routines in the case if petools is     *
! *   compiled without BLAS support. If a user process tries to access   *
! *   to one of these routines, they generate error message, unwind      *
! *   stack and abnorammly terminate.                                    *
! *                                                                      *
! *  ### 15-DEC-2006               v1.0 (c)  L. Petrov  15-DEC-2006 ###  *
! *                                                                      *
! ************************************************************************
      IMPLICIT   NONE 
      INTEGER*4  IARR(2), IND
      WRITE ( 6, '(A)' ) 'Trap of internal control: attempt to access '// &
     &                   'to blas routine DGBTRS. PETOOLS library was '// &
     &                   'configured without blas support'
      WRITE ( 6, '(A)' ) 'Abnoraml termination'
      IND = 3
      IND = IARR(IND) 
      CALL EXIT ( 1 )
      RETURN
      END SUBROUTINE DGBTRS
!
! ------------------------------------------------------------------------
!
      SUBROUTINE DGBTRS_()
! ************************************************************************
! *                                                                      *
! *   These are so-called BLAS stub routines. They are provided to       *
! *   resolve depdendeces on blas routines in the case if petools is     *
! *   compiled without BLAS support. If a user process tries to access   *
! *   to one of these routines, they generate error message, unwind      *
! *   stack and abnorammly terminate.                                    *
! *                                                                      *
! *  ### 15-DEC-2006               v1.0 (c)  L. Petrov  15-DEC-2006 ###  *
! *                                                                      *
! ************************************************************************
      IMPLICIT   NONE 
      INTEGER*4  IARR(2), IND
      WRITE ( 6, '(A)' ) 'Trap of internal control: attempt to access '// &
     &                   'to blas routine DGBTRS_. PETOOLS library was '// &
     &                   'configured without blas support'
      WRITE ( 6, '(A)' ) 'Abnoraml termination'
      IND = 3
      IND = IARR(IND) 
      CALL EXIT ( 1 )
      RETURN
      END SUBROUTINE DGBTRS_
!
! ------------------------------------------------------------------------
!
      SUBROUTINE SGBTRF()
! ************************************************************************
! *                                                                      *
! *   These are so-called BLAS stub routines. They are provided to       *
! *   resolve depdendeces on blas routines in the case if petools is     *
! *   compiled without BLAS support. If a user process tries to access   *
! *   to one of these routines, they generate error message, unwind      *
! *   stack and abnorammly terminate.                                    *
! *                                                                      *
! *  ### 15-DEC-2006               v1.0 (c)  L. Petrov  15-DEC-2006 ###  *
! *                                                                      *
! ************************************************************************
      IMPLICIT   NONE 
      INTEGER*4  IARR(2), IND
      WRITE ( 6, '(A)' ) 'Trap of internal control: attempt to access '// &
     &                   'to blas routine SGBTRF. PETOOLS library was '// &
     &                   'configured without blas support'
      WRITE ( 6, '(A)' ) 'Abnoraml termination'
      IND = 3
      IND = IARR(IND) 
      CALL EXIT ( 1 )
      RETURN
      END SUBROUTINE SGBTRF
!
! ------------------------------------------------------------------------
!
      SUBROUTINE SGBTRF_()
! ************************************************************************
! *                                                                      *
! *   These are so-called BLAS stub routines. They are provided to       *
! *   resolve depdendeces on blas routines in the case if petools is     *
! *   compiled without BLAS support. If a user process tries to access   *
! *   to one of these routines, they generate error message, unwind      *
! *   stack and abnorammly terminate.                                    *
! *                                                                      *
! *  ### 15-DEC-2006               v1.0 (c)  L. Petrov  15-DEC-2006 ###  *
! *                                                                      *
! ************************************************************************
      IMPLICIT   NONE 
      INTEGER*4  IARR(2), IND
      WRITE ( 6, '(A)' ) 'Trap of internal control: attempt to access '// &
     &                   'to blas routine SGBTRF_. PETOOLS library was '// &
     &                   'configured without blas support'
      WRITE ( 6, '(A)' ) 'Abnoraml termination'
      IND = 3
      IND = IARR(IND) 
      CALL EXIT ( 1 )
      RETURN
      END SUBROUTINE SGBTRF_
!
! ------------------------------------------------------------------------
!
      SUBROUTINE SGBTRS()
! ************************************************************************
! *                                                                      *
! *   These are so-called BLAS stub routines. They are provided to       *
! *   resolve depdendeces on blas routines in the case if petools is     *
! *   compiled without BLAS support. If a user process tries to access   *
! *   to one of these routines, they generate error message, unwind      *
! *   stack and abnorammly terminate.                                    *
! *                                                                      *
! *  ### 15-DEC-2006               v1.0 (c)  L. Petrov  15-DEC-2006 ###  *
! *                                                                      *
! ************************************************************************
      IMPLICIT   NONE 
      INTEGER*4  IARR(2), IND
      WRITE ( 6, '(A)' ) 'Trap of internal control: attempt to access '// &
     &                   'to blas routine SGBTRS. PETOOLS library was '// &
     &                   'configured without blas support'
      WRITE ( 6, '(A)' ) 'Abnoraml termination'
      IND = 3
      IND = IARR(IND) 
      CALL EXIT ( 1 )
      RETURN
      END SUBROUTINE SGBTRS
!
! ------------------------------------------------------------------------
!
      SUBROUTINE SGBTRS_()
! ************************************************************************
! *                                                                      *
! *   These are so-called BLAS stub routines. They are provided to       *
! *   resolve depdendeces on blas routines in the case if petools is     *
! *   compiled without BLAS support. If a user process tries to access   *
! *   to one of these routines, they generate error message, unwind      *
! *   stack and abnorammly terminate.                                    *
! *                                                                      *
! *  ### 15-DEC-2006               v1.0 (c)  L. Petrov  15-DEC-2006 ###  *
! *                                                                      *
! ************************************************************************
      IMPLICIT   NONE 
      INTEGER*4  IARR(2), IND
      WRITE ( 6, '(A)' ) 'Trap of internal control: attempt to access '// &
     &                   'to blas routine SGBTRS_. PETOOLS library was '// &
     &                   'configured without blas support'
      WRITE ( 6, '(A)' ) 'Abnoraml termination'
      IND = 3
      IND = IARR(IND) 
      CALL EXIT ( 1 )
      RETURN
      END SUBROUTINE SGBTRS_
!
! ------------------------------------------------------------------------
!
      SUBROUTINE XERBLA
      IMPLICIT   NONE 
      INTEGER*4  IARR(2), IND
      WRITE ( 6, '(A)' ) 'Trap of internal control: attempt to access '// &
     &                   'to blas routine XERBLA. PETOOLS library was '// &
     &                   'configured without blas support'
      WRITE ( 6, '(A)' ) 'Abnoraml termination'
      IND = 3
      IND = IARR(IND) 
      CALL EXIT ( 1 )
      RETURN
      END SUBROUTINE XERBLA
!
! ------------------------------------------------------------------------
!
      SUBROUTINE XERBLA_()
      IMPLICIT   NONE 
      INTEGER*4  IARR(2), IND
      WRITE ( 6, '(A)' ) 'Trap of internal control: attempt to access '// &
     &                   'to blas routine XERBLA. PETOOLS library was '// &
     &                   'configured without blas support'
      WRITE ( 6, '(A)' ) 'Abnoraml termination'
      IND = 3
      IND = IARR(IND) 
      CALL EXIT ( 1 )
      RETURN
      END SUBROUTINE XERBLA_
